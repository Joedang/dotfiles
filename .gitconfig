[user]
	name = Joedang
	email = shields6@pdx.edu
	signingkey = 5892B7FC2FBFB981
[credential]
	helper = cache --timeout=3600
[push]
	default = simple
[core]
	editor = vim
	excludesfile = ~/.gitignore
[merge]
	tool = vimdiff
	conflictstyle = diff3
[alias]
#	home = !cd "$(git rev-parse --show-toplevel)"
#	home = rev-parse --show-toplevel
	home = !pwd
#	gr = log --graph --full-history --all --color --abbrev-commit --decorate --date=short
#	gr = log --graph --full-history --all --color --format='%Cred%h%Creset %an (%G?) %Cblue%ai%Creset%d%n%s%n' 
	gr = log --graph --full-history --all --color --format='%C(auto)%h %an (%G?) %C(green)%ci%C(auto)%d%n%s%n' 
	gr-ch = log --graph --full-history -M --name-status --color --format='%C(auto)%h %an (%G?) %C(green)%ci%C(auto)%d%n%s%n' 
	sta = status
	k = !gitk --all
	vimdiff = difftool --tool=vimdiff 
	mt = mergetool
